# Resume Source

This folder contains the committed source for `Zack_Wilde_Resume.pdf`.

Regenerate the PDF from the repo root:

```powershell
powershell -ExecutionPolicy Bypass -File resume/build.ps1
```

Keep `resume/resume.html`, `Zack_Wilde_Resume.md`, and the portfolio claims in `index.html` in sync when changing resume content.
